{
	"name": "VizDoom CS-GY 6923",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"runArgs": [
		"--gpus", "all"
	],
	// No need to run postCreateCommand as it’s already done in Dockerfile
	"postStartCommand": "python /workspaces/rl_pacman/debug/check_dependencies.py"
	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}